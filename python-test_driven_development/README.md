# 0x07. Python - Test-driven Development

This directory contains Python functions created using the Test-Driven Development (TDD) approach.  
Each function includes strict input validation and follows Pythonic error handling.

---

## Files Description

- **0-add_integer.py**  
  Safely add two numbers as integers.

- **2-matrix_divided.py**  
  Divide all elements of a matrix by a number, with full validation and rounding to 2 decimals.

- **3-say_my_name.py**  
  Print `"My name is <first name> <last name>"` after validating input strings.

- **4-print_square.py**  
  Print a square of `#` characters of a given size.

- **5-text_indentation.py**  
  Print text with two new lines after `.`, `?`, and `:`.

- **6-max_integer.py**  
  Return the maximum integer in a list.

---

## How to Run Tests

This project uses **doctest**.

Run tests with:

```bash
python3 -m doctest -v ./tests/*
```
Requirements

Python 3.x

Code follows PEP8 style

All functions validated with doctest
