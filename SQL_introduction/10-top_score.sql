-- List all records from second_table showing score first and name second
-- Order the results by score in descending order (highest score first)
SELECT score, name
FROM second_table
ORDER BY score DESC;
